<!DOCTYPE html>
<html lang="en">

<head>
    <%- include ("./templates/head"); %>
</head>

<body>
    <div class="body-wrap">
        <div> <img src='<%= product.img %>' alt=<%=product.name %> ></div>
        <div>
            <%= product.name %>
        </div>
        <div>
            <%= product.price %>
        </div>
        <div>
            <%= product.description %>
        </div>
    </div>
</body>