<nav class="navbar">
    <div class="nav-left">
        <div class="nav-element">
            <h2 class="nav-h"> <a href="/"> SKLEP </a> </h2>
        </div>
    </div>
    <div class="nav-mid">
        <div class="nav-element">
            <% if (url=="/" ) { %>
                <h2 class="nav-h" id="active">
                    <% } else { %>
                        <h2 class="nav-h">
                            <% } %>
                                <a href="/">
                                    Produkty
                                </a>
                        </h2>
        </div>
        <div class="nav-element">
            <% if (url=="/search" ) { %>
                <h2 class="nav-h" id="active">
                    <% } else { %>
                        <h2 class="nav-h">
                            <% } %>
                                <a href="/search">
                                    Szukaj
                                </a>
                        </h2>
        </div>
    </div>

    <% if (locals.user) { %>
        <div class="nav-right">
            <div class="nav-element">
                <% if (url=="/cart" ) { %>
                    <h2 class="nav-h" id="active">
                        <% } else { %>
                            <h2 class="nav-h">
                                <% } %>
                                    <a href="/cart"> Koszyk(<%= Number(cart_item_count) %>) </a>
                            </h2>
            </div>
            <div class="nav-element">
                <% if (url=="/login" ) { %>
                    <h2 class="nav-h" id="active">
                        <% } else { %>
                            <h2 class="nav-h">
                                <% } %>
                                    <a href="/login">
                                        Konto <% locals.user.role %>
                                    </a>
                            </h2>
            </div>

            <% } else { %>
                <div class="nav-element">
                    <% if (url=="/login" ) { %>
                        <h2 class="nav-h" id="active">
                            <% } else { %>
                                <h2 class="nav-h">
                                    <% } %>
                                        <a href="/login">
                                            Logowanie
                                        </a>
                                </h2>
                </div>

                <% } %>
        </div>

</nav>